<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="sjz.sgy.lb.dao.template.TemplateMapper" >


<!--  -->
	<select id="queryTemplateList" resultType="map">
		SELECT
		id AS templateId,
		template_name AS urlName,
		UNIX_TIMESTAMP(create_time) AS createTime,
		UNIX_TIMESTAMP(modify_time) AS modifyTime,
		template_path AS zipUrl
		FROM
		template WHERE del_state = 1

	</select>

	<insert id="addTemplate" parameterType="map">
		INSERT INTO
		template
		(
		
		template_name,
		template_path,
        create_time,
		del_state
		)
		VALUES
		(#{urlName},#{zipUrl},now() ,1)


	</insert>

<update id="delTemplate" parameterType="map">
		UPDATE template
		SET
		del_state = 2
		WHERE id = #{templateId}
	</update>



  
</mapper>